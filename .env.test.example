# Copy to .env.test and fill in from the *dedicated test* Supabase project
# (Settings -> Database -> Connection string -> URI tab, Transaction pooler,
# same shape/gotchas as DATABASE_URL in .env.example — see that file).
# Never point this at the dev/prod project: internal/testutil.OpenTestTx
# runs real INSERT/UPDATE/DELETE statements against it (rolled back after
# each test, but only safe because this database is disposable).
TEST_DATABASE_URL=
